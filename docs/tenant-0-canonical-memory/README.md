# Tenant 0 Canonical Memory Seed

## Purpose

Seed canonical memory entries for `tenant_0` in the schema defined by [memory-schema.md](/home/mcintosh/repos/hetzner-clawbot/docs/memory-schema.md).

These files are initial examples and durable starting points for the future tenant-aware memory layer.

## Scope

Current seed set:

- one tenant-shared entry
- one `Stacks` bot-private entry
- one `Jennifer` bot-private entry
- one `Bob` bot-private entry

## Notes

- these are canonical-memory examples, not runtime state
- they are intentionally small and high-confidence
- they are suitable for later migration into `/opt/clawbot/tenants/tenant_0/memory/canonical/`
